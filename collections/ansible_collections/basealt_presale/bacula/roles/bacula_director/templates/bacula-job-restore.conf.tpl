Job {
  Name = {{item.name}}
  Client = {{item.client}}
  Type = {{item.type}}
  FileSet = {{item.fileset}}
  Storage = {{item.storage}}
  Messages = {{item.messages}}
  Priority = {{item.priority}}
  Pool = {{item.pool}}
  Where = {{item.where}}
}
