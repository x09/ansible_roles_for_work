Client {
  Name = {{item.name}}
  Address = {{item.address}}
  FDPort = {{item.port}}
  Catalog = {{item.catalog}}
@{{item.pass_file}}
  File Retention = {{item.file_retention}}
  Job Retention = {{item.job_retention}}
  AutoPrune = {{item.autoprune}}
}
