Pool {
  Name = {{item.name}}
  Pool Type = {{item.backup_type}}
  Recycle = {{item.recycle}}
  AutoPrune = {{item.autoprune}}
  Volume Retention = {{item.volume_retention}}
  LabelFormat = "{{item.label_format}}"
}
